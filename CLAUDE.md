# CLAUDE.md

## 先讀 AGENTS.md

**本專案的工作規則全部在 [`AGENTS.md`](AGENTS.md)，開工前先讀完。**
那裡有：絕對禁止事項（不可憑記憶寫營業時間／座標）、標準流程、五類常犯錯、
回報規則、環境細節。這份檔案只補 AGENTS.md 沒寫的東西，不重複。

`DECISIONS.md` 存決策理由，`HANDOFF.md` 存外部待確認事項（會落後，行程以 `trip.js` 為準）。

---

## 省 context：這個專案最容易浪費 input 的三件事

長對話會被自動摘要，摘要會掉細節。真正的成本不是 token 數字，是**細節遺失後又改錯一次**。
實測下來，吃掉 input 的前三名不是對話內容：

### 1. 在 scratchpad 建 `.py` 腳本檔（最大一筆）

只要腳本檔留在磁碟上，之後任何一次變動（自己 patch、或別的 session 動到），
系統就會把**整份 100+ 行 Python 回噴一次**。單次數千 token，一輪對話可能發生 5–6 次。

**改用一次性 heredoc，不留檔：**
```bash
python - <<'PYEOF'
# ...
PYEOF
```
需要反覆執行的才寫成檔案，寫了就在同一輪用完刪掉。

### 2. 整段 dump `trip.js` 的 note

單一站點的 `note` 常有 500–800 字。要確認內容時**用 grep 抓需要的欄位**，
不要 `sed -n` 印整個 day block：
```bash
# 好：只要時間與站名
grep -o 't:"[^"]*", kind:"[a-z]*", name:"[^"]*"' trip.js
# 壞：整段 note 全印
```

### 3. 別的 session 同時在改檔

這個 repo 常有多個 session 並行。**動手前先 `git log --oneline -3` ＋
確認 `SITE_VERSION`**，不要拿記憶中的版本當現況——這次就發生過三次
「我看到的已經過期」（以為還是 v88，實際 v108）。

---

## 我自己犯過、要特別注意的

- **`.gitattributes` 統一 LF**。用 Python 改檔時不要寫 `newline="\r\n"`。
- **行程調整先給差異表（改前→改後＋理由）等確認再動檔案。**
  AGENTS.md 已明載，但很容易在連續對話中忘記。
- **查營業時間用 `verify_places.py`**，不要自己用網頁搜尋後填進 data。
  自行查證填錯過三次（プリンセスピピ、ミディアムレア、三麗鷗）。
- **同名店家**：先確認地址再填座標。`ステーキハウス ミディアムレア` 在西戸崎與
  博多駅南各有一家，差 10 km；`由布まぶし 心` 的金鱗湖本店與駅前支店定休不同。
  需要消歧義就在該筆加 `q` 欄位。

---

## 使用者偏好

- 回覆用繁體中文。
- **只講現況，不講修改歷程**——這條同時適用於站點 note 與對話回覆。
- 所有查到的問題都要講，包含判斷為無害的，由使用者決定。
- 明確標示資訊來源等級：①API 實查 ②官網 ③推測 ④查不到。
